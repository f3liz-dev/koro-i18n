{
  "name": "koro-i18n",
  "private": true,
  "version": "0.1.0",
  "description": "Lightweight i18n translation management platform",
  "scripts": {
    "setup": "cd server && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd server && npm run build && cd ../frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
